<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inicio de Sesión</title>
  <link rel="icon" href="resources/img/LogoMaicarSinFondoBLANCO.ico" type="image/x-icon">
  <link rel="stylesheet" href="resources/css/Login/bootstrap.min.css">
  <link rel="stylesheet" href="resources/css/Login/login.css">
</head>

<body>
  <div class="container w-500 mt-5">
    <div class="row align-items-stretch">
      <div class="col bg d-none d-lg-block col-md-5 col-lg-5 col-xl-6 rounded">
        <p class="fw-bold my-5 text-center display-5">¡Bienvenido de Nuevo!</p>
      </div>
      <div class="col p-5">
        <div class="text-end">
          <img src="resources/img/LogoMaicarSinFondo.png" width="100">
        </div>
        <h2 class="fw-bold pt-5">Iniciar Sesión</h2>
        <p class="py-4">¡Bienvenido de Nuevo! Ingrese a su cuenta.</p>

        <!-- Login -->
        <form action="/auth" method="POST">
          <div class="form-floating">
            <input type="text" class="form-control border border-dark" name="nombreUsuario" id="nombreUsuario"
              placeholder="Usuario">
            <label for="nombreUsuario">Usuario</label>
          </div>
          <div class="form-floating my-4">
            <input type="password" class="form-control border border-dark" name="contraseña" id="contraseña"
              placeholder="Password">
            <label for="contrasena">Contraseña</label>
          </div>

          <div class="form-check text-start my-3">
            <input class="form-check-input" type="checkbox" value="remember-me" id="flexCheckDefault">
            <label class="form-check-label" for="flexCheckDefault">
              Recordar Sesión
            </label>
          </div>

          <div class="d-grid py-5 text-center">
            <button type="submit" class="btn btn-primary">Iniciar Sesión</button>
            <div class="py-2">
              <span><a href="#">¿Se te ha olvidado la contraseña?</a></span>
            </div>
          </div>
          <div>
            <span>¿Nuevo Usuario? <a href="/registro">Regístrate</a></span>
          </div>
        </form>

      </div>
    </div>
  </div>


  <script src="/components/bootstrap/bootstrap.bundle.min.js"></script>


  <!-- BACKEND ALERTA DE INICIO DE SESION. -->
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
  <% if (typeof alert !="undefined" ) { %>
    <script>
      Swal.fire({
        title: '<%= alertTitle %>',
        text: '<%= alertMessage %>',
        icon: '<%= alertIcon %>',
        showConfirmButton: '<%= showConfirmButton %>',
        timer: '<%= timer %>'
      }).then(() => {
        window.location = '/<%= ruta %>';
      });
    </script>
    <% } %>
</body>

</html>